sudo: required

language: bash

services:
- docker

arch:
  script:
  - echo "hello from arch"
  - "./install.sh test"

script:
- echo "hello from outside"
- "./tests/arch-travis.sh"
- echo "ciao from outside"
