sudo: required

language: bash

services:
- docker

arch:
  script:
  - echo "hello from arch"
  - "./csvInstall.sh test"
  - echo "ciao from arch"

script:
- echo "hello from outside"
- "./arch-travis.sh"
- echo "ciao from outside"
